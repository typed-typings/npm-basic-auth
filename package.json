{
  "name": "npm-basic-auth",
  "version": "1.0.0",
  "private": true,
  "description": "Typings for basic-auth",
  "scripts": {
    "build": "typings bundle -o test/bundle.d.ts",
    "test": "tsc -p test",
    "lint": "tslint -c tslint.json index.d.ts lib/**/*.d.ts test/**/*.ts"
  },
  "author": "Felix Becker <felix.b@outlook.com>",
  "license": "ISC",
  "devDependencies": {
    "tslint": "^4.0.2",
    "typescript": "^2.2.1",
    "typings": "^2.0.0"
  }
}
